apiVersion: v1
kind: Service
metadata:
  name: tenant-db-service
spec:
  type: NodePort
  selector:
    app: tenant-db
  ports:
    - name: default
      port: 5432
      targetPort: 5432
      nodePort: 30604