apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: marketplace-gateway
spec:
  controllerName: marketplace-gateway-controller
  listeners:
  - name: http
    protocol: HTTP
    port: 80